/* Dev Dojo configuration. Dev Dojo is the root course, so it keeps the engine's
   default name and home copy; this file exists to credit its alpha testers on
   the home screen via the shared engine (DOJO_ALPHA), the same way the sibling
   dojos do. Add a person the day they finish, with their sign-off on the text. */
const DOJO_ALPHA = [
  { name: 'Seve Zavala', url: 'https://www.linkedin.com/in/seve-zavala' },
];
