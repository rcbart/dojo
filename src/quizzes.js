window.QUIZZES={"jvm1":[{"q":"Why does a local int live in the stack frame rather than on the heap?","options":["Its lifetime ends when the frame pops, so no collector is needed","Primitives are too small for the collector to track individually","The heap only accepts values that implement a common base type","The JIT relocates primitives to the stack once a method turns hot"],"answer":0,"why":"A frame is pushed on call and popped on return, so the storage is reclaimed by the return itself.","whyWrong":["","Size is not the criterion. Objects of any size live on the heap.","The heap holds objects and arrays, with no base-type requirement.","JIT compilation changes how code runs, not where a declared local lives."]},{"q":"A method runs int[] data = new int[3]. Where do the variable and the array live?","options":["Both on the heap, since the array makes the declaration an object","The variable in the frame, the array on the heap","Both in the frame, because the array is created inside the method","The variable on the heap, the array in the metaspace region"],"answer":1,"why":"The reference is a local like any other, and every array object is allocated on the heap.","whyWrong":["The variable holding the reference is still a local in the frame.","","Where an array is created has no bearing. Arrays are heap objects.","Metaspace holds class metadata, not the arrays your code allocates."]},{"q":"What does Java's pass-by-value mean when the argument is an object?","options":["The object itself is copied, so the callee works on its own instance","Nothing is copied, and the callee reads the caller's frame directly","The reference is copied, so both names point at the same object","The reference is shared, so reassigning it in the callee is visible"],"answer":2,"why":"Java copies the value you pass, and for an object that value is the reference, so both frames point at one heap object.","whyWrong":["No copy of the object is made. Only the reference is copied.","Each call gets its own frame, and the callee never reads the caller's locals.","","Reassigning the parameter changes only the callee's copy of the reference."]},{"q":"A program dies with StackOverflowError. Which memory area ran out?","options":["The metaspace region holding the loaded class definitions","The shared heap, once live objects outgrew what it can hold","The operand stack inside a single frame overflowed its slots","The calling thread's own stack, exhausted by nested frames"],"answer":3,"why":"Each call pushes a frame, and recursion that never terminates keeps pushing until the thread's stack is exhausted.","whyWrong":["Running out of metaspace reports itself as an OutOfMemoryError.","That is the heap failure, and it reports OutOfMemoryError.","Operand stack depth is fixed per method and checked when the class is verified.",""]},{"q":"Why are local variables thread-safe without any synchronization?","options":["Each thread gets its own stack, so no frame is shared","The JVM adds a lock around every frame that it pushes","Locals are immutable once the frame has been pushed","The garbage collector serializes access to local slots"],"answer":0,"why":"There is one stack per thread, so no other thread can reach a frame's local variables.","whyWrong":["","No lock is taken. Pushing a frame is a pointer move.","Locals are reassigned freely. It is isolation, not immutability.","The collector manages the heap and does not arbitrate local access."]},{"q":"What does javac produce, and what does the JIT do with it later?","options":["Native code for the build machine, which the JIT then verifies at load","Bytecode, which the JIT rewrites into a smaller bytecode form","Bytecode, which the JIT compiles to native code for hot methods","Native code for every platform, which the JIT picks at startup"],"answer":2,"why":"javac emits portable bytecode. The JVM interprets it and JIT-compiles the methods that run often.","whyWrong":["javac never emits machine code, which is what makes the class file portable.","The JIT emits native code, not another round of bytecode.","","One class file serves every platform. The JVM on each is what differs."]}],"ds6":[{"q":"What does Theta(n log n) claim that O(n log n) does not?","options":["That the algorithm beats any O(n log n) alternative","That n log n is both the upper and the lower bound","That the bound holds for every input, not just the worst","That the constant factors were measured, not assumed"],"answer":1,"why":"Theta means O and Omega agree, so the growth is pinned from both sides rather than capped from above.","whyWrong":["Notation describes growth, not a comparison between implementations.","","Which case you mean is stated separately from the notation.","All three notations discard constant factors."]},{"q":"Quicksort is Theta(n log n) on average and O(n^2) at worst. Why both?","options":["Theta applies to sorted input and Big-O to random input","Theta counts comparisons where Big-O also counts the swaps","The bound and the case being described are separate labels","Big-O is for the algorithm and Theta for one implementation"],"answer":2,"why":"One statement is about the average case and the other about the worst, where a bad pivot splits off one element at a time.","whyWrong":["Sorted input is the classic worst case for a naive pivot, not the average one.","Neither notation is tied to a particular operation you count.","","Both describe the algorithm. Neither is about a specific implementation."]},{"q":"Why is ArrayList.add called amortized O(1) rather than average O(1)?","options":["The average is over inputs, and add takes no input to average","Amortized is the term reserved for any operation that copies memory","Average O(1) would be a claim about the best case only","It is a guarantee about a sequence, not a hopeful distribution"],"answer":3,"why":"Doubling the capacity spreads one O(n) copy across the adds that follow, so any run of adds is linear in total.","whyWrong":["Average case does apply to operations. That is not what separates the two.","The copy is not what earns the term. The guarantee over a sequence is.","Average case is about the distribution of inputs, not the best case.",""]},{"q":"A loop over n items contains a loop over a fixed 10 items. What is the cost?","options":["O(n), because the inner count is a constant factor","O(n^2), because any nested loop multiplies to quadratic","O(10n), since the constant belongs in the final answer","O(n log n), because the inner loop bounds the outer one"],"answer":0,"why":"Nested loops multiply, and n times 10 is 10n, which drops its constant to O(n).","whyWrong":["","Nesting multiplies the two counts. It does not automatically square n.","Constant factors are dropped, which is the point of the notation.","Nothing here halves the work, so no logarithm appears."]},{"q":"Why can an O(n) algorithm lose to an O(n^2) one on real data?","options":["Because Big-O is only valid for inputs above a million items","Asymptotics drop constants, and the constant can be large","Because O(n^2) is a worst case that real inputs never reach","Because memory access is not counted as an operation at all"],"answer":1,"why":"Insertion sort beats quicksort under roughly 40 items, which is why real sort implementations switch to it for small partitions.","whyWrong":["There is no size threshold below which the notation stops applying.","","Worst cases do occur, and the point holds even when they do not.","Memory access is work like any other. Complexity does not exempt it."]},{"q":"Binary search is O(log n). What in the code makes it logarithmic?","options":["It compares each element against the target exactly once","It uses a sorted array, and sorted access is always log time","It calls itself, and recursion costs the log of the depth","Each step discards half of what is left to search"],"answer":3,"why":"Halving repeatedly takes about log2 n steps: a thousand items in ten, a million in twenty.","whyWrong":["Touching every element once would be linear, which is the opposite.","Sorting enables the halving. Being sorted is not itself a time bound.","The iterative version has no recursion and runs in the same time.",""]}],"ds7":[{"q":"Why does a plain BST degrade to O(n) when you insert sorted keys?","options":["Sorted keys hash to the same bucket, so the nodes collide","Comparisons stop discarding branches once keys repeat","Every key goes right, so the tree becomes a linked list","The tree rebalances after each insert and never converges"],"answer":2,"why":"The logarithm comes from height. A chain of nodes has height n, so a search walks all of them.","whyWrong":["A BST does not hash anything. It compares keys.","Duplicate keys are not the issue. Strictly increasing keys are.","","A plain BST does not rebalance at all, which is the problem."]},{"q":"What makes a trie lookup independent of how many keys are stored?","options":["It walks one node per character of the key","It keeps every key in a single sorted array of chars","It hashes the key once and jumps straight to the node","It compares the key against the median key at each step"],"answer":0,"why":"Cost is the length of the key you are looking up, so adding more keys does not lengthen the walk.","whyWrong":["","There is no shared array. The structure is a tree of character nodes.","That describes a hash table, which cannot answer prefix queries.","That is a balanced BST, where cost does grow with the key count."]},{"q":"Why do database indexes use B-trees rather than binary search trees?","options":["Binary trees cannot store keys larger than a machine word","B-trees keep the keys sorted where binary trees do not","Binary trees have to be rebuilt whenever a row is deleted","Wide nodes mean fewer levels, so fewer disk reads"],"answer":3,"why":"Each level down the tree is a disk read, and packing many keys into a node keeps the tree shallow.","whyWrong":["Key size is not the constraint. Either structure can hold large keys.","A binary search tree keeps its keys ordered too.","Deletion is a local operation in both, not a rebuild.",""]},{"q":"You need the ten largest values from a stream of millions. What fits?","options":["A trie, since it orders keys as they arrive","A heap of size ten, evicting the smallest","A hash table keyed by the value itself","A sorted array rebuilt after every insert"],"answer":1,"why":"A heap gives O(1) access to its extreme and O(log n) insert, so top-k costs a single pass.","whyWrong":["A trie orders by character prefix, which says nothing about magnitude.","","A hash table has no ordering to pull the largest from.","Re-sorting on each insert is the cost the heap exists to avoid."]},{"q":"What does a TreeMap give you that a HashMap does not?","options":["Constant time lookup regardless of the key count","Automatic eviction once the map reaches capacity","Sorted keys, so range queries are possible","Thread safety, since rotations happen atomically"],"answer":2,"why":"TreeMap is a red-black tree, so the keys stay in order and you can ask for everything between two of them.","whyWrong":["That is the HashMap strength. A tree lookup is O(log n).","Neither map evicts entries on its own.","","TreeMap is not thread-safe, and its rotations are not atomic."]}],"ds8":[{"q":"Why does BFS find the shortest path in an unweighted graph when DFS does not?","options":["DFS visits some nodes twice, inflating the path it reports","BFS sorts the neighbors before it visits any of them","DFS cannot detect that it has reached the goal node","BFS reaches every node at its true distance first"],"answer":3,"why":"The queue expands nodes in order of distance, so the first time BFS sees a node it has arrived by a shortest route.","whyWrong":["A visited set stops revisits. The problem is the order of exploration.","No sorting happens, and neighbor order does not fix a depth-first descent.","DFS finds the goal fine. The path it took is just not the shortest.",""]},{"q":"What about Dijkstra requires a priority queue rather than a plain queue?","options":["It has to revisit nodes, and only a heap allows that","It must expand the closest unvisited node next","It stores edges rather than nodes, and edges need ordering","It runs backwards from the goal, so the order is reversed"],"answer":1,"why":"The greedy step is only correct if you always settle the node with the smallest known distance, and a heap makes finding it cheap.","whyWrong":["Once a node is settled Dijkstra does not revisit it.","","It settles nodes. Edges are relaxed as it goes.","It runs forward from the source unless you reverse the graph yourself."]},{"q":"Why can Dijkstra give a wrong answer when an edge weight is negative?","options":["It settles a node before a cheaper route can appear","Negative weights make the priority queue reject the entry","The distances become negative and underflow the counter","It cannot compare a negative weight against a positive one"],"answer":0,"why":"Once the closest node is settled Dijkstra never reconsiders it, and a negative edge found later can undercut that distance. Bellman-Ford handles those graphs.","whyWrong":["","A heap orders negative numbers as happily as positive ones.","Nothing overflows. The arithmetic is ordinary addition.","Comparing the two is straightforward arithmetic."]},{"q":"You need a build order where every dependency comes before what needs it. Which algorithm?","options":["Dijkstra, since the cheapest task should run first","BFS, since it visits each level before the next one","A topological sort over the dependency graph","DFS from every node, keeping the longest chain found"],"answer":2,"why":"A topological sort orders a DAG so every edge points forward, which is exactly the build-dependency rule.","whyWrong":["There is no cost to minimize. The constraint is order, not weight.","Level order ignores dependencies that cross levels.","","The longest chain is the critical path, not a valid full ordering."]},{"q":"DFS is described as using a stack. Where is the stack in the recursive version?","options":["In the visited set, which pops as the search unwinds","It is the call stack, one frame per call","The JVM converts the recursion into an explicit stack","There is none, which is why recursion can miss nodes"],"answer":1,"why":"Recursion pushes a frame per node, so the call stack holds the path back to where the search started.","whyWrong":["The visited set records where you have been. It does not order the backtrack.","","The JVM runs the recursion as written. It does not rewrite it.","The call stack is the stack, and DFS reaches every reachable node."]},{"q":"Both BFS and DFS on a graph are O(V+E). What is that counting?","options":["Every pair of nodes that had to be compared","The number of levels times the branching factor","The height of the search tree plus its total width","Each node once, and each edge as it is followed"],"answer":3,"why":"With a visited set you touch each node once and walk each edge once, which is what the sum says.","whyWrong":["Neither search compares nodes pairwise.","That product overcounts and does not hold for irregular graphs.","Height and width describe a shape, not the work done.",""]}],"web9":[{"q":"Why can offset pagination show the same row twice or skip one entirely?","options":["A row inserted or deleted above shifts the whole window","The database returns rows in a different order each query","The offset counts pages, and page size can change per request","The client caches the previous page and merges it with the next"],"answer":0,"why":"Offset is a position in a count. Insert a row near the top between requests and every later row moves down one, so the item at the boundary is never sent.","whyWrong":["","Order is stable once you sort. The shifting count is the problem.","Changing the page size is a client mistake, not the cause of the drift.","No caching is involved. The rows themselves have moved."]},{"q":"Why is a cursor encoded as an opaque value instead of a plain row id?","options":["Encoding it stops an attacker reading another user's rows","An id would let the client re-sort the results itself","It keeps the sort key from becoming a public contract","The server needs it encrypted to validate it on the next call"],"answer":2,"why":"If clients can parse and build cursors, the ordering you chose is frozen into an interface you can never change.","whyWrong":["Encoding is not authorization. The server still has to check access.","Reading a cursor does not let a client reorder a server-side page.","","Base64 is not encryption, and the server validates the cursor either way."]}],"web10":[{"q":"Which change forces a new major version of an API?","options":["Adding an optional request field that clients may omit","Changing a field's type from a string to a number","Adding an endpoint alongside the existing ones","Adding a response field that old clients ignore"],"answer":1,"why":"A type change breaks strict parsers even though the field name is untouched. Additive changes do not.","whyWrong":["An optional field an old client never sends changes nothing for it.","","A new endpoint is invisible to clients that do not call it.","A tolerant reader ignores fields it does not know about."]},{"q":"What is the argument against putting the version in the URI path?","options":["Proxies strip unknown path segments before routing","It cannot express more than one major version at a time","Logs record the path, so the version leaks to anyone","The same resource ends up with more than one URL"],"answer":3,"why":"Purists object that a resource should have one identifier. The path is still the most common choice because it is visible and trivial to route.","whyWrong":["Proxies pass the path through. That is not a real failure mode.","Several versions live side by side under different paths.","Visibility in logs is an argument for the path, not against it.",""]},{"q":"What do the Deprecation and Sunset response headers tell a client?","options":["That this version is going away, and by when","That the request was rejected for using an old version","That the server will translate old calls to the new shape","That the client must send a version header on every call"],"answer":0,"why":"They announce a retirement in the response itself, so a client learns about it from traffic it is already sending.","whyWrong":["","They are advisory. The request is still served normally.","They announce a date. They do not perform any translation.","They say nothing about how a client should send its version."]}],"con0":[{"q":"Two threads sit in one process, next to two separate processes. What is shared?","options":["Both share the heap, and only the stacks stay private","Neither shares memory without explicit setup by the OS","The threads share a heap, and the processes share nothing","The threads share stacks, and the processes share their heaps"],"answer":2,"why":"Threads in one process see the same objects. Separate processes are isolated and need deliberate inter-process communication.","whyWrong":["Separate processes do not share a heap.","Threads inside one process share the heap by default.","","Each thread has its own stack, and processes stay isolated."]},{"q":"Why do race conditions plague threads but rarely separate processes?","options":["Processes run one at a time, so they cannot interleave","Threads write the same objects, and processes cannot","The OS locks a process's memory while it is running","Threads are scheduled more often, so timing gets tight"],"answer":1,"why":"A race needs two paths of execution reaching the same mutable state, and the shared heap is what gives threads that reach.","whyWrong":["Processes run concurrently too, on separate cores where there are any.","","Nothing is locked. The memory is simply not addressable from outside.","Scheduling frequency is not the difference. Shared state is."]}],"con7":[{"q":"A test passes a thousand times and fails once under load. What does that point at?","options":["A race on shared mutable state, dependent on timing","A deadlock that clears itself once a lock finally times out","A memory leak that only shows itself at high volume","A compiler bug that appears under optimization"],"answer":0,"why":"Non-determinism under load is the signature of unsynchronized access to state that two threads share.","whyWrong":["","A deadlock stops progress and stays stopped. It would not pass most runs.","A leak degrades steadily rather than failing one run in a thousand.","Blaming the compiler before ruling out your own shared state is rarely right."]},{"q":"Why is a race condition called a heisenbug?","options":["It disappears once the process is restarted cleanly","Two threads observe two different values at once","It can only be found by a tool, never by reading code","Adding a log line shifts the timing and hides it"],"answer":3,"why":"Attaching a debugger or logging changes the interleaving, so the act of observing removes the conditions that cause the bug.","whyWrong":["Plenty of bugs survive a restart, and a race can too.","Divergent reads are a symptom, not what the nickname describes.","Reading the code for unguarded shared state often finds it.",""]},{"q":"Threads are frozen and making no progress. Which tool answers it fastest?","options":["jcstress, to replay the memory model interleavings","jstack, which names the deadlocked threads and locks","A profiler, to see which method is consuming the CPU","A heap dump, to find the object the threads are holding"],"answer":1,"why":"A thread dump prints every stack, and the JVM labels a Java-level deadlock outright with the threads and locks involved.","whyWrong":["jcstress stress-tests concurrent code. It does not inspect a stuck process.","","Deadlocked threads burn no CPU, so a profiler shows nothing.","The heap shows objects, not which thread is waiting on which lock."]},{"q":"N threads each increment a shared counter once. The total comes out low. Why?","options":["The JVM drops increments once the threads contend","Each thread keeps a private copy and never publishes the result","Increment is read, modify and write, and it interleaves","The counter overflows and wraps back toward zero"],"answer":2,"why":"Two threads can read the same value, add one to it, and write the same result, so one of the increments is lost.","whyWrong":["Nothing in the JVM discards writes. The writes overwrite each other.","There is one counter. The problem is that both threads reach it.","","Wrapping needs billions of increments and would not explain a small shortfall."]},{"q":"How do you make an intermittent race reproducible enough to fix?","options":["Loop it across many threads and assert an invariant","Run the failing test once with the debugger attached","Raise the log level and wait for the failure to recur","Pin the process to one core so the threads cannot race"],"answer":0,"why":"Widen the window with a tight loop and small sleeps, then check something that must always hold, such as the total matching the thread count.","whyWrong":["","The debugger changes the timing, which is what hid the bug.","Waiting is not a method, and the extra logging shifts the timing too.","A single core still interleaves threads at every context switch."]},{"q":"What does swapping a plain int counter for an AtomicInteger actually fix?","options":["It stops other threads reading the counter mid-update","It makes read, modify and write one indivisible step","It copies the counter per thread and sums them at the end","It orders the threads so they increment in turn"],"answer":1,"why":"The compare-and-set underneath makes the whole increment atomic, so no update can be lost.","whyWrong":["Reads are not blocked. The fix is that no update goes missing.","","That is roughly what LongAdder does, by a different mechanism.","Order is not fixed. Only the atomicity of each increment is."]}],"pat8":[{"q":"What tells you a class has become a god object?","options":["It has more private fields than public methods","It is instantiated from many places in the codebase","It sits at the root of a deep inheritance tree","Every change to the system touches it"],"answer":3,"why":"Low cohesion is the symptom. Unrelated responsibilities in one class mean every feature has to edit it.","whyWrong":["The field-to-method ratio says nothing about how many jobs a class has.","A widely used class can still do exactly one thing.","Deep hierarchies are their own problem, and a god object needs no parent.",""]},{"q":"Why is a check like status == 3 worth fixing even though it works?","options":["Integer comparison is slower than comparing an enum","The compiler cannot check the range of a raw literal","The number carries no meaning for the next reader","A literal cannot be changed without a recompile step"],"answer":2,"why":"A named constant or an enum puts the meaning in the code, so nobody has to hunt for what 3 stood for.","whyWrong":["Speed is not the issue, and the difference would not matter anyway.","It cannot check a named constant's range either.","","So does a constant. Both are compiled in."]},{"q":"You are about to copy a block of logic for the second time. What is the rule?","options":["Copy it once more, then extract when there are three","Extract it into a shared method now","Leave it, since duplication is cheaper than a wrong shape","Move both copies into a shared base class to inherit from"],"answer":1,"why":"Refactor on the second copy, before the two versions drift apart and each one gets its own separate fix.","whyWrong":["Waiting for a third copy gives the first two time to diverge.","","Duplication is cheap to write and expensive to keep correct.","Inheritance for reuse ties both callers to one hierarchy."]},{"q":"Why does global mutable state show up as flaky tests?","options":["One test leaves state behind that the next one reads","Global fields are slower to access than injected ones","The test runner cannot see a static field to reset it","Static initialization runs in an unpredictable order"],"answer":0,"why":"The dependency is hidden, so test order starts to matter and a green suite depends on which test ran first.","whyWrong":["","Access speed has nothing to do with a test failing.","It can be reset. Remembering to reset it everywhere is the burden.","Initialization happens on first use, and leftover state is the usual cause."]},{"q":"You suspect a method is slow and want to make it faster. What comes first?","options":["Rewrite the inner loop to avoid allocation","Cache the result, since the call is probably repeated","Add an index, which is the usual bottleneck anyway","Measure, so you optimize the real hot spot"],"answer":3,"why":"Clever code with no profiler data behind it is premature optimization, and the cost is usually somewhere you did not guess.","whyWrong":["That is a guess about the cause, made before there is any evidence.","Caching adds an invalidation problem to a slowness you have not located.","Sometimes true, and still a guess until a measurement says so.",""]}]};
